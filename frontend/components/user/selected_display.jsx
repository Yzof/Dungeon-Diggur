import React from 'react';

const selectedDisplay = (props) => {
// This should display the select users comments/galleries/likes
  return (
    <div>

    </div>
  );
};

export default selectedDisplay;
